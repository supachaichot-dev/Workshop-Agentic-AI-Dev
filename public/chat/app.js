const history=[];const messages=document.querySelector('#messages');
function render(role,text){const el=document.createElement('div');el.className=`bubble ${role}`;el.textContent=text;messages.append(el);messages.scrollTop=messages.scrollHeight;}
document.querySelector('form').addEventListener('submit',async e=>{e.preventDefault();const input=document.querySelector('#message');const text=input.value.trim();if(!text)return;input.value='';render('user',text);history.push({role:'user',content:text});try{const r=await fetch('/api/chat',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({message:text,history:history.slice(0,-1),provider:document.querySelector('#provider').value,model:document.querySelector('#model').value})});const data=await r.json();render('assistant',data.reply||data.error||'เกิดข้อผิดพลาด');if(data.reply)history.push({role:'assistant',content:data.reply});}catch(err){render('assistant','เชื่อมต่อ backend ไม่สำเร็จ');}});